// This file is not used in Technician App
// Service booking pages are only for User App

import 'package:flutter/material.dart';

class MicrowavePage extends StatelessWidget {
  const MicrowavePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Service booking not available in Technician App'),
      ),
    );
  }
}
